name: Reusable workflow example

on:
  workflow_call:
    inputs:
      env:
        required: true
        type: string

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: |
           echo "Building on ${{ inputs.env }}"
           
      - name: Unit Test
        run: |
           echo "Unit Test on ${{ inputs.env }}"
           
      - name: Deployment
        run: |
           echo "Deployment on ${{ inputs.env }}"
